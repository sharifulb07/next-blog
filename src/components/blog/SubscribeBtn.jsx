'use client'

export default function SubscribeBtn({title}) {
  return <button onClick={() => {}} className="p-3 bg-[#FB743E] cursor-pointer font-poppins font-medium text-[16px] text-white">{title}</button>;
}
